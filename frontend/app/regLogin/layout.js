
import Header from "../components/Header"
export default function  Reglog({children})
{
    return(
        <section>
            <Header/>
           
            {children}
        </section> 
      )
}